{
  "name": "arky-site",
  "version": "0.1.0",
  "private": true,
  "description": "Arky Foundation static site, built with Astro and deployed to Cloudflare Pages.",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "bun run build:core && bun install && astro build",
    "build:core": "cd ../packages/core && bun install && bun run build",
    "build:site": "astro build",
    "preview": "astro preview",
    "lint": "oxlint -D correctness -D suspicious -D pedantic src/ public/scripts/",
    "fmt": "oxfmt --write src/ public/scripts/"
  },
  "dependencies": {
    "@arky/core": "file:../packages/core",
    "astro": "^7.0.0"
  },
  "devDependencies": {
    "oxlint": "^1.71.0",
    "oxfmt": "^0.56.0"
  }
}
